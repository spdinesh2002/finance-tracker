services:
  - type: web
    name: finance-tracker
    runtime: node
    buildCommand: npm install && npm run build:web
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
